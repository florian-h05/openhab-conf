uid: amplifierPlayback
description: control playback and display playback information of an amplifier
author: Copyright (c) 2021 Florian Hotze under MIT License
tags: []
props:
  parameters:
    - description: Header big sized
      label: Header
      name: header
      required: false
      type: TEXT
    - description: HEX or rgba
      label: Background Color
      name: bgcolor
      required: false
      type: TEXT
    - context: item
      description: Item for power
      label: Power
      name: item_power
      required: true
      type: TEXT
    - context: item
      description: Item for title
      label: Title
      name: item_title
      required: true
      type: TEXT
    - context: item
      description: Item for album
      label: Album
      name: item_album
      required: true
      type: TEXT
    - context: item
      description: Item for artist
      label: Artist
      name: item_artist
      required: true
      type: TEXT
    - context: item
      description: Item for playback control
      label: Playback control
      name: item_playback
      required: true
      type: TEXT
    - description: false for Yamaha Receiver binding, true for Yamaha MusicCast binding
      label: Playback command mode
      name: musiccast
      required: false
      type: BOOLEAN
      default: false
  parameterGroups: []
timestamp: Jul 20, 2021, 3:49:45 PM
component: f7-card
config:
  style:
    noShadow: false
    padding: 0px
    border-radius: var(--f7-card-expandable-border-radius)
    box-shadow: 5px 5px 10px 1px rgba(0,0,0,0.1)
    background-color: "=props.bgcolor ? props.bgcolor : ''"
    height: 150px
    min-width: 220px
    margin-left: 5px
    margin-right: 5px
slots:
  content:
    - component: f7-block
      config:
        style:
          position: absolute
          top: -5px
          flex-direction: row
          width: 50%
        class:
          - text-align-left
      slots:
        default:
          - component: Label
            config:
              text: =props.header
              style:
                font-size: 22px
                margin-top: 0px
                font-weight: 600
    - component: f7-block
      config:
        visible: "=items[props.item_power].state == 'ON' ? true : false"
        style:
          position: absolute
          top: 62px
          flex-direction: row
          width: 100%
        class:
          - text-align-left
      slots:
        default:
          - component: Label
            config:
              text: =items[props.item_title].state
              style:
                color: "#dd4124"
                font-size: 15px
                margin-top: 0px
                font-weight: 400
    - component: f7-block
      config:
        visible: "=items[props.item_power].state == 'ON' ? true : false"
        style:
          position: absolute
          top: 92px
          flex-direction: row
          width: 100%
        class:
          - text-align-left
      slots:
        default:
          - component: Label
            config:
              text: =items[props.item_artist].state
              style:
                font-size: 13px
                margin-top: 0px
                font-weight: 300
    - component: f7-block
      config:
        visible: "=items[props.item_power].state == 'ON' ? true : false"
        style:
          position: absolute
          top: 128px
          flex-direction: row
          width: 100%
        class:
          - text-align-left
      slots:
        default:
          - component: Label
            config:
              text: =items[props.item_album].state
              style:
                font-size: 11px
                margin-top: 0px
                font-weight: 300
    - component: f7-block
      config:
        style:
          position: absolute
          top: 75px
          flex-direction: row
          width: 100%
        class:
          - text-align-center
      slots:
        default:
          - component: Label
            config:
              visible: "=items[props.item_power].state === 'OFF' ? true : false"
              text: Bitte anschalten!
              style:
                font-size: 22px
                margin-top: 0px
                font-weight: 400
    - null
    - component: oh-button
      config:
        visible: "=items[props.item_power].state == 'ON' ? true : false"
        round: true
        large: false
        iconF7: lessthan_circle
        iconColor: "=themeOptions.dark === 'dark' ? 'white' : 'black'"
        iconSize: 30px
        action: command
        actionItem: =props.item_playback
        actionCommand: "=props.musiccast == true ? 'PREVIOUS' : 'Previous'"
        style:
          position: absolute
          top: 15px
          right: 80px
          height: 34px
    - component: oh-button
      config:
        visible: "=items[props.item_power].state == 'ON' ? (items[props.item_playback].state !== 'Pause' ? (items[props.item_playback].state !== 'PAUSE' ? true : false) : false) : false"
        round: true
        large: false
        iconF7: pause_circle
        iconColor: "=themeOptions.dark === 'dark' ? 'white' : 'black'"
        iconSize: 30px
        action: command
        actionItem: =props.item_playback
        actionCommand: "=props.musiccast == true ? 'PAUSE' : 'Pause'"
        style:
          position: absolute
          top: 15px
          right: 45px
          height: 34px
    - component: oh-button
      config:
        visible: "=items[props.item_power].state == 'ON' ? (items[props.item_playback].state === 'Pause' ? true : (items[props.item_playback].state === 'PAUSE' ? true : false)) : false"
        round: true
        large: false
        iconF7: play_circle
        iconColor: "=themeOptions.dark === 'dark' ? 'white' : 'black'"
        iconSize: 30px
        action: command
        actionItem: =props.item_playback
        actionCommand: "=props.musiccast == true ? 'PLAY' : 'Play'"
        style:
          position: absolute
          top: 15px
          right: 45px
          height: 34px
    - component: oh-button
      config:
        visible: "=items[props.item_power].state == 'ON' ? true : false"
        round: true
        large: false
        iconF7: greaterthan_circle
        iconColor: "=themeOptions.dark === 'dark' ? 'white' : 'black'"
        iconSize: 30px
        action: command
        actionItem: =props.item_playback
        actionCommand: "=props.musiccast == true ? 'NEXT' : 'Next'"
        style:
          position: absolute
          top: 15px
          right: 10px
          height: 34px
